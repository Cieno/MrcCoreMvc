@model MRCDataLibrary._02_Models.AttendanceModel

<h1>Delete</h1>

@if (Model is null)
{
    <h4>The requested record was not found.</h4>
}
else
{
    <div class="h4">
        Are you sure you want to delete @Model.MemberName's attendence on @Model.WorshipDate.ToString("yyyy-MM-dd")?
    </div>
    <form asp-action="Delete">
        <input asp-for="@Model.WorshipId" type="hidden" />
        <input asp-for="@Model.MemberId" type="hidden" />
        <input type="submit" value="Delete" class="btn btn-danger" />
        <a class="btn btn-secondary" href="/Attendance/AttendanceList?worshipId=@Model.WorshipId">Cancel</a>
        @*<a href="/Attendance/AttendanceList?worshipId=@Model.WORSHIP_ID">Cancel</a>*@
    </form>
}